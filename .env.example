# Public backend origin for the frontend (no secrets)
# Used to call Render backend public endpoints (e.g. /api/public/news)
NEXT_PUBLIC_API_ORIGIN=https://newspulse-backend-real.onrender.com

# Legacy fallback (optional). Prefer NEXT_PUBLIC_API_ORIGIN.
NEXT_PUBLIC_API_BASE_URL=https://newspulse-backend-real.onrender.com
